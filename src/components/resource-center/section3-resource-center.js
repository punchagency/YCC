import { Box } from '@mui/material'

const Section3ResourceCenter = () => {
    // This section is now intentionally left empty as per the new design.
    return <Box />;
}

export default Section3ResourceCenter;
